@model IEnumerable<Hafiz136.Models.Teacher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.T_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_Designation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_Cell)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.T_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.T_Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.T_Cell)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
